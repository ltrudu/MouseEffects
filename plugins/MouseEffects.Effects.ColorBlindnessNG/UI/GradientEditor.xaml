<UserControl x:Class="MouseEffects.Effects.ColorBlindnessNG.UI.GradientEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="32"
             MinHeight="32">
    <Grid>
        <!-- Gradient bar with rounded corners -->
        <Border x:Name="GradientBorder" CornerRadius="4" BorderThickness="1" BorderBrush="#555"
                ClipToBounds="True">
            <Image x:Name="GradientImage" Stretch="Fill"/>
        </Border>

        <!-- Handle canvas for draggable endpoints -->
        <Canvas x:Name="HandleCanvas"
                Background="Transparent"
                MouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                MouseLeftButtonUp="Canvas_MouseLeftButtonUp"
                MouseMove="Canvas_MouseMove"
                MouseLeave="Canvas_MouseLeave"/>
    </Grid>
</UserControl>
