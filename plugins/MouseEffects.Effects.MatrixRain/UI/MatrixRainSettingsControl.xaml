<UserControl x:Class="MouseEffects.Effects.MatrixRain.UI.MatrixRainSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="400">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

            <!-- Column Settings -->
            <TextBlock Text="Column Settings" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

            <TextBlock Text="Column Density" Margin="0,5,0,2"/>
            <DockPanel Margin="0,0,0,10">
                <TextBlock DockPanel.Dock="Right" Width="50" Text="{Binding Value, ElementName=ColumnDensitySlider, StringFormat=F3}" TextAlignment="Right" Margin="5,0,0,0"/>
                <Slider x:Name="ColumnDensitySlider" Minimum="0.01" Maximum="0.1" Value="0.04" TickFrequency="0.005" IsSnapToTickEnabled="True" ValueChanged="ColumnDensitySlider_Changed"/>
            </DockPanel>

            <Separator Margin="0,10"/>

            <!-- Speed Settings -->
            <TextBlock Text="Fall Speed" FontWeight="Bold" FontSize="14" Margin="0,10,0,10"/>

            <TextBlock Text="Min Fall Speed (px/s)" Margin="0,5,0,2"/>
            <DockPanel Margin="0,0,0,10">
                <TextBlock DockPanel.Dock="Right" Width="50" Text="{Binding Value, ElementName=MinSpeedSlider, StringFormat=F0}" TextAlignment="Right" Margin="5,0,0,0"/>
                <Slider x:Name="MinSpeedSlider" Minimum="50" Maximum="500" Value="100" TickFrequency="10" IsSnapToTickEnabled="True" ValueChanged="MinSpeedSlider_Changed"/>
            </DockPanel>

            <TextBlock Text="Max Fall Speed (px/s)" Margin="0,5,0,2"/>
            <DockPanel Margin="0,0,0,10">
                <TextBlock DockPanel.Dock="Right" Width="50" Text="{Binding Value, ElementName=MaxSpeedSlider, StringFormat=F0}" TextAlignment="Right" Margin="5,0,0,0"/>
                <Slider x:Name="MaxSpeedSlider" Minimum="100" Maximum="800" Value="300" TickFrequency="10" IsSnapToTickEnabled="True" ValueChanged="MaxSpeedSlider_Changed"/>
            </DockPanel>

            <Separator Margin="0,10"/>

            <!-- Character Settings -->
            <TextBlock Text="Character Settings" FontWeight="Bold" FontSize="14" Margin="0,10,0,10"/>

            <TextBlock Text="Character Change Rate" Margin="0,5,0,2"/>
            <DockPanel Margin="0,0,0,10">
                <TextBlock DockPanel.Dock="Right" Width="50" Text="{Binding Value, ElementName=CharChangeSlider, StringFormat=F1}" TextAlignment="Right" Margin="5,0,0,0"/>
                <Slider x:Name="CharChangeSlider" Minimum="1" Maximum="30" Value="8" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="CharChangeSlider_Changed"/>
            </DockPanel>

            <Separator Margin="0,10"/>

            <!-- Appearance Settings -->
            <TextBlock Text="Appearance" FontWeight="Bold" FontSize="14" Margin="0,10,0,10"/>

            <TextBlock Text="Glow Intensity" Margin="0,5,0,2"/>
            <DockPanel Margin="0,0,0,10">
                <TextBlock DockPanel.Dock="Right" Width="50" Text="{Binding Value, ElementName=GlowSlider, StringFormat=F1}" TextAlignment="Right" Margin="5,0,0,0"/>
                <Slider x:Name="GlowSlider" Minimum="0" Maximum="3" Value="1.2" TickFrequency="0.1" IsSnapToTickEnabled="True" ValueChanged="GlowSlider_Changed"/>
            </DockPanel>

            <TextBlock Text="Trail Length" Margin="0,5,0,2"/>
            <DockPanel Margin="0,0,0,10">
                <TextBlock DockPanel.Dock="Right" Width="50" Text="{Binding Value, ElementName=TrailSlider, StringFormat=F2}" TextAlignment="Right" Margin="5,0,0,0"/>
                <Slider x:Name="TrailSlider" Minimum="0.1" Maximum="1" Value="0.7" TickFrequency="0.05" IsSnapToTickEnabled="True" ValueChanged="TrailSlider_Changed"/>
            </DockPanel>

            <TextBlock Text="Effect Radius (px)" Margin="0,5,0,2"/>
            <DockPanel Margin="0,0,0,10">
                <TextBlock DockPanel.Dock="Right" Width="50" Text="{Binding Value, ElementName=RadiusSlider, StringFormat=F0}" TextAlignment="Right" Margin="5,0,0,0"/>
                <Slider x:Name="RadiusSlider" Minimum="100" Maximum="800" Value="300" TickFrequency="10" IsSnapToTickEnabled="True" ValueChanged="RadiusSlider_Changed"/>
            </DockPanel>

            <Separator Margin="0,10"/>

            <!-- Color Settings -->
            <TextBlock Text="Color" FontWeight="Bold" FontSize="14" Margin="0,10,0,10"/>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                    <TextBlock Text="Red" Margin="0,0,0,2"/>
                    <Slider x:Name="ColorRSlider" Minimum="0" Maximum="1" Value="0.2" TickFrequency="0.01" ValueChanged="ColorSlider_Changed"/>
                    <TextBlock Text="{Binding Value, ElementName=ColorRSlider, StringFormat=F2}" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5,0">
                    <TextBlock Text="Green" Margin="0,0,0,2"/>
                    <Slider x:Name="ColorGSlider" Minimum="0" Maximum="1" Value="1" TickFrequency="0.01" ValueChanged="ColorSlider_Changed"/>
                    <TextBlock Text="{Binding Value, ElementName=ColorGSlider, StringFormat=F2}" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="5,0,0,0">
                    <TextBlock Text="Blue" Margin="0,0,0,2"/>
                    <Slider x:Name="ColorBSlider" Minimum="0" Maximum="1" Value="0.3" TickFrequency="0.01" ValueChanged="ColorSlider_Changed"/>
                    <TextBlock Text="{Binding Value, ElementName=ColorBSlider, StringFormat=F2}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <Border x:Name="ColorPreview" Height="30" Background="#33FF4D" CornerRadius="3" Margin="0,5,0,10"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
